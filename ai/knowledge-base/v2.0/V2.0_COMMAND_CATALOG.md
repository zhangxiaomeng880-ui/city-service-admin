# AI Native Project OS V2.0 — Command Catalog

## 1. Purpose

This document defines the standard command entry layer for V2.0. Every exposed capability MUST be discoverable and executable without relying on a UI.

The same command contract is intended to be consumed by:

- Human command / chat interface
- Agent
- Workflow
- API
- SDK
- MCP / Tool adapters
- Future UI

Core flow:

`Query / Retrieval → Context Assembly → Command → Execution → Validation / Audit → Trace → Data Asset`

## 2. Standard action vocabulary

All resource operations use the standard Action vocabulary:

- `Create` — create a resource
- `Update` — modify a resource
- `Execute` — execute a capability or resource action
- `Publish` — make a validated version/configuration effective
- `Terminate` — stop an active execution
- `Delete` — remove a resource when policy permits

A resource does not need to support every Action. Support is determined by Resource Contract, Lifecycle, Action Policy and Permission.

## 3. Command naming convention

Commands use:

`<resource>.<action>`

Examples:

- `project.create`
- `project.query`
- `resource.tree`
- `requirement.reference`
- `test_case.create`
- `test.execute`
- `audit.execute`
- `model.list`
- `model.update`
- `routing.execute`

Commands that are discovery-oriented may use a capability-specific suffix such as `query`, `tree`, or `search`; these are mapped to Query / Retrieval capabilities rather than creating a second action vocabulary.

## 4. Core command catalog

### Project

| Command | Purpose | Typical input | Output |
|---|---|---|---|
| `project.query` | Query project status/details | project_id / filters | Project resource |
| `project.create` | Create project from template | Project Create Template | Project + initial version/iteration |
| `project.update` | Update project configuration | project_id + patch | Updated Project |
| `project.publish` | Publish project/version | project_id + version | Published version |
| `project.terminate` | Terminate project execution | project_id + reason | Termination record |
| `project.delete` | Delete project if policy permits | project_id | Deletion record |

### Resource discovery and reuse

| Command | Purpose | Typical input | Output |
|---|---|---|---|
| `resource.tree` | Build lightweight AI Asset Tree | project_id / iteration_id | Tree of resources + key properties |
| `resource.query` | Query structured resources | resource_type + filters | Resource list |
| `resource.retrieve` | Retrieve resource content/context | resource_id | Resource content/context |
| `resource.reference` | Reuse an existing resource by reference | source_resource_id + target | Reference relation |
| `resource.clone` | Clone an existing resource | source_resource_id + target | New resource |
| `resource.fork` | Fork an existing resource/version | source_resource_id + target | Forked resource/version |

### Requirement / Design / Technical Solution

| Command | Purpose |
|---|---|
| `requirement.create` | Create requirement |
| `requirement.update` | Update requirement |
| `requirement.reference` | Reuse existing requirement |
| `design.create` | Create design asset |
| `design.update` | Update design asset |
| `design.reference` | Reuse existing design |
| `technical_solution.create` | Create technical solution |
| `technical_solution.update` | Update technical solution |
| `technical_solution.reference` | Reuse existing technical solution |

### Code

| Command | Purpose |
|---|---|
| `code.query` | Query repository / branch / commit context |
| `code.retrieve` | Retrieve relevant code/context |
| `code.execute` | Execute coding task |
| `code.publish` | Publish approved code change |
| `code.terminate` | Terminate active coding execution |

### Testing

| Command | Purpose |
|---|---|
| `test_case.create` | Create structured test cases |
| `test_case.update` | Update test cases |
| `test_case.reference` | Reuse existing test cases |
| `test.execute` | Execute test cases / test suite |
| `test.terminate` | Stop active test execution |
| `test.report` | Generate structured test report |

Testing coverage should support, where applicable: visual, functional, performance, API/interface, boundary values, data tracking/reporting, and compliance-related testing.

### Audit / Governance

| Command | Purpose |
|---|---|
| `audit.query` | Query audit rules/results |
| `audit.execute` | Execute independent audit |
| `audit.report` | Generate audit report |
| `validation.execute` | Execute validation |
| `gate.execute` | Evaluate a lifecycle gate |

Audit is independent from the stage execution it audits and replaces the old stage-level review mechanism where the V2.0 governance model applies.

### Model / Tool / MCP / Skill

| Command | Purpose |
|---|---|
| `model.list` | Query model pool |
| `model.query` | Query model capability/configuration |
| `model.create` | Register a model |
| `model.update` | Update model configuration |
| `model.publish` | Publish model configuration/version |
| `tool.list` | Query tool pool |
| `tool.create` | Register a tool |
| `tool.update` | Update tool configuration |
| `mcp.list` | Query MCP pool |
| `mcp.create` | Register MCP |
| `skill.list` | Query skill pool |
| `skill.create` | Register skill |

### Routing

| Command | Purpose |
|---|---|
| `routing.query` | Query routing rules/configuration |
| `routing.update` | Update routing policy |
| `routing.score` | Calculate candidate routing score |
| `routing.execute` | Select execution capability according to policy |
| `routing.trace` | Query routing decision trace |

Routing rules include user-specified model priority, default model, failure fallback, dynamic routing, quality/consumption balance, routing budget, high-concurrency constraints and traceability.

## 5. Generic command contract

Every command MUST expose the following metadata:

```yaml
command_id: <string>
resource_type: <string>
action: <Create|Update|Execute|Publish|Terminate|Delete|Query|Retrieve|Discovery>
description: <string>
permission: <permission expression>
input_schema: <schema reference>
required_fields: []
optional_fields: []
preconditions: []
execution_mode: <sync|async|stream>
output_schema: <schema reference>
error_schema: <schema reference>
trace_schema: <schema reference>
audit_required: <true|false>
```

## 6. Trigger methods

A command can be triggered through multiple adapters while keeping the same underlying contract:

1. **Command** — explicit command invocation.
2. **Natural language** — AI maps user intent to a registered command.
3. **Event** — system event invokes a command.
4. **Schedule** — scheduled execution invokes a command.
5. **UI** — future UI calls the same command/API layer.
6. **API / SDK** — external systems invoke the command contract directly.

UI is therefore an adapter, not a prerequisite for execution.

## 7. Natural-language resolution rule

When a user gives a natural-language instruction:

`User Intent → Query/Retrieval → Resource Resolution → Command Resolution → Permission Check → Input Validation → Execute → Trace`

The Agent MUST prefer existing resources when a reusable resource is available. It should use the AI Asset Tree to expose candidate resources when user selection is needed.

## 8. Project creation command

`project.create` uses the complete Project Create Template.

All standard project fields MUST resolve to a value, but the value may come from:

- User input
- Existing resource reference
- Template default
- AI inference requiring user confirmation
- System-generated value

Project planning inputs include start/end time, phase configuration, budget, risk configuration, scope, resources and historical project data.

The Planning Engine generates an initial project plan and a confidence assessment before final confirmation.

## 9. Execution record

Every executable command MUST create an execution record containing at minimum:

- execution_id
- command_id
- project_id / resource_id when applicable
- actor / agent
- selected model / tool / MCP / skill
- routing decision
- start/end time
- status
- token / usage / cost data when applicable
- input/output references
- issue references
- validation/audit result
- trace_id

These records are project data assets and must be queryable and archivable.

## 10. Error and failure behavior

- User-specified model has the highest priority. If the user-specified model fails, report failure to the user rather than silently switching models.
- Default model failure may enter the configured routing mechanism.
- Routing decisions must be traceable.
- Permission failure, validation failure, precondition failure and execution failure must remain distinguishable.
- Termination must preserve the execution trace.

## 11. Command discoverability

The system MUST expose a queryable Command Catalog so that an Agent, developer or user can determine:

- What commands exist
- What resource each command operates on
- What Action it represents
- Required inputs
- Permission requirements
- Expected outputs
- Failure conditions
- Whether audit is required
- Whether execution is synchronous/asynchronous

This document is the human-readable catalog. API/SDK schemas should reference the same command identifiers and contracts rather than defining a second incompatible command system.

## 12. Relationship to V2.0

`V2.0 Capability → Command Contract → API / SDK → Agent / Workflow / UI / External System`

The Command Catalog is the executable bridge between the PASS capability foundation and the subsequent API / SDK implementation phase.
