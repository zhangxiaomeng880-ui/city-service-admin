schema: AuditGateResult
version: 1.0
required: [audit_gate_result_id, audit_id, phase_id, status, blocking, evaluated_at]
fields:
  audit_gate_result_id: string
  audit_id: string
  project_id: string
  phase_id: string
  status: string
  blocking: boolean
  evidence_ids: [string]
  decision_reason: string
  handoff_allowed: boolean
  evaluated_at: datetime
